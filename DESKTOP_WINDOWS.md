# برنامج معرض حور لنظام Windows

يحتوي المشروع على نسخة سطح مكتب مستقلة مبنية باستخدام Electron. عند تشغيلها تُفتح واجهة معرض حور داخل نافذة برنامج Windows، ويبدأ خادم محلي داخل البرنامج، وتُحفظ قاعدة البيانات SQLite داخل مجلد بيانات المستخدم في Windows بدل الاعتماد على MySQL أو اتصال إنترنت.

## إنتاج ملف التثبيت

يُشغَّل البناء تلقائيًا عند الدفع إلى فرع `main` أو يدويًا من تبويب **Actions** في GitHub عبر workflow باسم **Build Hawr Gallery for Windows**. يجب أن تنجح مرحلة فحص TypeScript والاختبارات أولًا، ثم تُرفع ملفات المثبت تحت Artifact باسم `HawrGallery-Windows-Installer`. الملف الرئيسي يكون باسم قريب من `HawrGallery-Setup-1.0.0.exe`.

## التثبيت

نزّل Artifact وفك ضغطه، ثم شغّل ملف `HawrGallery-Setup-*.exe` على جهاز Windows. يسمح المثبت بتحديد مجلد التثبيت وينشئ اختصارًا على سطح المكتب وقائمة Start. لا يحتاج البرنامج إلى MySQL أو Node.js أو Manus Desktop بعد التثبيت.

## مكان البيانات والنسخ الاحتياطي

تُحفظ البيانات تلقائيًا في مجلد بيانات التطبيق تحت `%APPDATA%` داخل مجلد البرنامج، وتحديدًا في ملف `data/hawr-gallery.sqlite`. لا تحذف هذا الملف إذا كانت لديك بيانات مهمة.

من قائمة البرنامج اختر **ملف → نسخ احتياطي للبيانات** لحفظ نسخة `.sqlite` في المستندات أو في قرص خارجي. للاستعادة اختر **ملف → استعادة نسخة احتياطية**، ثم أغلق البرنامج وافتحه مرة أخرى بعد نجاح الاستعادة. يوصى بعمل نسخة احتياطية يومية على وسيط منفصل.

## تشغيل الهاتف عبر Wi‑Fi

هذه النسخة محلية على Windows. يمكن إضافة الوصول من الهاتف عبر الشبكة المحلية لاحقًا، لكن يجب أن يبقى الكمبيوتر قيد التشغيل، وأن يسمح جدار حماية Windows بالمنفذ المحلي. لا يُنصح بفتح المنفذ على الإنترنت العام دون إضافة مصادقة وشهادة HTTPS.

## أوامر المطور

```bash
pnpm desktop:dev
pnpm desktop:build
pnpm desktop:dir
```

الأمر `desktop:build` مخصص لإنتاج مثبت Windows، ويُفضَّل تشغيله داخل GitHub Actions على `windows-latest` لأن `better-sqlite3` يعتمد على وحدة أصلية تُبنى حسب نظام التشغيل.

## الهوية البصرية والتوقيع الرقمي

يستخدم المثبت الآن `assets/hawr-icon.ico` كأيقونة للتطبيق والاختصارات، ويتم توليد ملفات PNG وICO تلقائيًا عبر `pnpm assets:build` قبل بناء Windows.

لدعم التوقيع الرقمي في GitHub Actions، أضف سرين إلى إعدادات المستودع باسم `WIN_CSC_LINK` و`WIN_CSC_KEY_PASSWORD`. السر الأول هو رابط Base64 أو ملف شهادة Code Signing بصيغة PFX، والثاني كلمة مرور الشهادة. عند عدم إضافة السرّين سيستمر البناء، لكنه سيكون غير موقّع وقد يعرض Windows تحذير SmartScreen.

## الإعداد الأولي

عند أول تشغيل تظهر نافذة إعداد عربية تسمح بتحديد اسم المعرض وتفعيل تذكير النسخ الاحتياطي. تُحفظ هذه الإعدادات داخل مجلد بيانات المستخدم في Windows، بينما تبقى قاعدة SQLite محلية ولا تحتاج إلى اتصال بالإنترنت للعمليات الأساسية.

## ربط الهاتف عبر Wi‑Fi

بعد تشغيل البرنامج، افتح قائمة **اتصال الهاتف ← عرض رابط الربط عبر Wi‑Fi**. اتصل الهاتف والكمبيوتر بنفس شبكة Wi‑Fi، ثم افتح الرابط المنسوخ من نافذة البرنامج على الهاتف. يمنح الرابط الهاتف جلسة محلية لمدة 30 يومًا، ويظل الخادم محميًا لطلبات الهاتف إلى أن يتم فتح رابط الاقتران الصحيح. يتغير رمز الاقتران عند إعادة تشغيل البرنامج، ويمكن إيقاف الوصول ببساطة بإغلاق البرنامج أو تغيير شبكة Wi‑Fi.

> لا يحتاج الهاتف إلى تطبيق Android منفصل في هذه المرحلة؛ يمكن تثبيت واجهة النظام على الشاشة الرئيسية من متصفح الهاتف، بينما تظل قاعدة البيانات على كمبيوتر Windows.


## الإصدار 1.1.0: الدخول والتحديث وربط الهاتف

عند أول تشغيل بعد التثبيت تظهر شاشة إنشاء حساب المدير. أدخل اسم المدير، ثم كودًا إنجليزيًا من ثلاثة أحرف أو أرقام على الأقل، وكلمة مرور من ستة أحرف أو أكثر. تُخزّن كلمة المرور في SQLite على هيئة hash مع salt ولا تُحفظ كنص صريح. بعد ذلك تظهر شاشة تسجيل الدخول في كل تشغيل، ولا يمكن فتح إدارة الموظفين أو تغيير بياناتهم إلا بعد دخول المدير.

يتحقق البرنامج تلقائيًا من وجود إصدار أحدث في GitHub Releases، ويبدأ تنزيله في الخلفية. عند اكتمال التنزيل يُثبت تلقائيًا عند إغلاق البرنامج أو يمكن اختيار «إعادة التشغيل والتثبيت» من شريط التحديث. لا تُجبر النسخة البرنامج على الإغلاق أثناء إدخال فاتورة حتى لا تضيع عملية غير محفوظة.

لربط الهاتف، يجب أن يكون الهاتف والكمبيوتر على نفس شبكة Wi‑Fi، ثم اختر من البرنامج «اتصال الهاتف ← عرض QR ورابط الربط». يستخدم البرنامج المنفذ 3688، وقد ينتقل تلقائيًا إلى أحد المنافذ التالية حتى 3707 إذا كان المنفذ مشغولًا. يتضمن المثبت قاعدة جدار حماية لشبكات Windows المصنفة Private. إذا ظهر `ERR_ADDRESS_UNREACHABLE`، أوقف بيانات الهاتف مؤقتًا، تأكد أن الهاتف على نفس الراوتر وليس شبكة ضيوف، غيّر Network profile في Windows إلى Private، واسمح لبرنامج «معرض حور» عبر Windows Firewall. لا تستخدم عنوان `127.0.0.1` في الهاتف؛ استخدم عنوان الشبكة الذي يظهر في شريط الربط، مثل `192.168.1.23:3688`.

تتوفر أزرار التصدير في المخزون والمبيعات والتوريد والتقارير بصيغتي Excel وPDF. تُصدر القوائم الحالية بعد تطبيق البحث والفلاتر، ولا تُضاف أي بيانات غير محفوظة إلى الملفات.
