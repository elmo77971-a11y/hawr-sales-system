export type LookupProduct = { sku: string; barcode?: string | null; name?: string; id: number };
export function findProductByCode<T extends LookupProduct>(products: T[], code: string) { const normalized = code.trim().toLowerCase(); if (!normalized) return undefined; return products.find(product => product.sku.toLowerCase() === normalized || product.barcode?.toLowerCase() === normalized) || products.find(product => product.name?.toLowerCase() === normalized) || products.find(product => product.name?.toLowerCase().includes(normalized)); }
export function netLineTotal(unitPrice: number, quantity: number, discount: number) { return Math.max(0, Number((unitPrice * quantity - discount).toFixed(2))); }
